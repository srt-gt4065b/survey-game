# 🎮 김교수님의 게임화 설문 시스템

## 🚀 빠른 시작 가이드

### 1️⃣ 프로젝트 다운로드
```bash
# 이 폴더를 김교수님 컴퓨터로 복사하세요
```

### 2️⃣ 의존성 설치
```bash
cd survey-game
npm install
```

### 3️⃣ Firebase 설정
1. [Firebase Console](https://console.firebase.google.com) 접속
2. 새 프로젝트 생성 또는 기존 프로젝트 사용
3. 프로젝트 설정 > 일반 > 내 앱 > 웹 앱 추가
4. Firebase SDK 설정값 복사
5. `src/firebase/config.js` 파일 열어서 설정값 붙여넣기

### 4️⃣ 실행
```bash
npm start
```
브라우저가 자동으로 열리고 http://localhost:3000 에서 실행됩니다!

## 📁 CSV 파일 형식

### 기본 형식
```csv
section,question_text,type,required,options
학업만족도,수업 내용의 질에 대해 어떻게 평가하십니까?,likert,yes,매우불만족|불만족|보통|만족|매우만족
학업만족도,교수진의 강의 방식에 만족하십니까?,likert,yes,매우불만족|불만족|보통|만족|매우만족
시설만족도,도서관 시설에 만족하십니까?,likert,yes,매우불만족|불만족|보통|만족|매우만족
```

### 질문 타입
- `likert`: 5점 척도 (만족도)
- `yesno`: 예/아니오
- `multiple`: 다중 선택
- `text`: 주관식
- `frequency`: 빈도 (자주/가끔/전혀)

## 🎮 게임 요소

### 포인트 시스템
- 기본 답변: 10 XP
- 빠른 답변 (10초 이내): +5 XP
- 연속 5개 답변: x1.5 보너스
- 연속 10개 답변: x2.0 보너스

### 레벨 시스템
- 100 XP마다 1레벨 상승
- 레벨업 시 50 코인 보상
- 레벨업 시 축하 애니메이션

### 업적 시스템
- 🏅 첫 답변 (10 pts)
- ⬆️ 레벨업 (50 pts)
- 🔥 연속 5개 (30 pts)
- 💥 연속 10개 (50 pts)
- 👑 연속 20개 (100 pts)
- ⚡ 스피드 악마 (40 pts)
- 💎 완벽주의자 (80 pts)

### 배지 컬렉션
- 🐤 얼리버드 (오전 9시 이전)
- 🦉 올빼미 (밤 10시 이후)
- ⚔️ 주말 전사 (주말 참여)
- 🏃 스피드스터 (빠른 완료)
- ✅ 완성주의자 (전체 완료)

## 📊 관리자 기능

### Firebase Console에서 확인
1. Firestore Database에서 실시간 응답 확인
2. Analytics에서 사용자 행동 분석
3. 실시간 리더보드 모니터링

### 데이터 다운로드
Firebase Console > Firestore > 컬렉션 선택 > 내보내기

## 🎨 커스터마이징

### 색상 변경
`src/App.css`에서 gradient 색상 변경:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### 질문 개수 변경
`src/store/gameStore.js`에서:
```javascript
totalQuestions: 150, // 원하는 개수로 변경
```

### 챕터 크기 변경
`src/components/SurveyGame.js`에서:
```javascript
if (nextIndex % 15 === 0) // 15를 원하는 숫자로 변경
```

## 🚀 배포 (GitHub Pages)

### 1. GitHub 레포지토리 생성
### 2. package.json에 추가:
```json
"homepage": "https://김교수님깃허브아이디.github.io/survey-game"
```

### 3. 배포 명령어:
```bash
npm run build
npm install --save-dev gh-pages
npm run deploy
```

## 🔥 Firebase 배포

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

## 📱 모바일 최적화
- 반응형 디자인 적용됨
- 터치 제스처 지원
- PWA 설정 가능

## 🎯 문제 해결

### npm 에러 발생 시
```bash
npm cache clean --force
rm -rf node_modules
npm install
```

### Firebase 연결 에러
1. Firebase 프로젝트 설정 확인
2. 인증 규칙 확인 (개발 중엔 테스트 모드 사용)
3. API 키 확인

## 🎉 완성!

이제 학생들이 재미있게 설문에 참여할 수 있습니다!

### 특징:
- ✅ 150개 질문 CSV 업로드
- ✅ 실시간 리더보드
- ✅ 포인트 & 레벨 시스템
- ✅ 업적 & 배지
- ✅ 모바일 반응형
- ✅ 오프라인 지원
- ✅ 자동 저장

---
💻 개발: Claude & 김교수님
🎮 게임화 설문의 새로운 패러다임!
